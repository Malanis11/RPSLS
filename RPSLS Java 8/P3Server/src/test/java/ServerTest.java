import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

class ServerTest {
	
	Server serverConnection;
	
	@Test
	void constr_test() {
	
		//serverConnection = new Server(a->{}, b->{}, c->{}, d->{}, 5555);
		
	//	assertEquals("Server", serverConnection.getClass().getName(), "Class Constructor Does Not Work");
	}

}